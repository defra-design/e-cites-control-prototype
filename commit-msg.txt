Add smooth notification banner close with no page jump

- Add CSS transition for max-height, opacity, margin and padding on dismissable
  banners so they collapse and fade out over 0.3s when Close is clicked
- Add _notification-banner-close-script.njk: on Close click, animate banner out,
  remove from DOM, and update URL via history.replaceState (no full reload)
- Mark all three banners (endorsed, changes saved, permit not endorsed) with
  data-dismissable and data-close-banner for consistent behaviour
- Avoids previous jump caused by full reload and hash scroll to #endorsement-section
